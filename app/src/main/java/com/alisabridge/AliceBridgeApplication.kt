package com.alisabridge

import android.app.Application

class AliceBridgeApplication : Application() {
    override fun onCreate() {
        super.onCreate()
        // Initialize app-wide components if needed
    }
}
